/** @type {import('next').NextConfig} */
const nextConfig = {
    //basePath: "/koreanisch-lernen",
    output: "export",  // <=== enables static exports
    //reactStrictMode: true,
  };
  
  export default nextConfig;